<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MaBoutique - Historique des Commandes</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <% include partials/header %>
    <main>
        <section>
            <h2>Historique des Commandes</h2>
            <% if (orders.length > 0) { %>
                <ul>
                    <% orders.forEach(order => { %>
                        <li>
                            Commande #<%= order.id %> - Total: €<%= order.total %> - Statut: <%= order.statut %> - Date: <%= order.date_commande %>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>Aucune commande passée.</p>
            <% } %>
        </section>
    </main>
    <% include partials/footer %>
</body>
</html>


// Placez ici les scripts JavaScript nécessaires pour les interactions de l'interface administrateur
document.addEventListener('DOMContentLoaded', (event) => {
    console.log('Admin interface ready');
    // Ajoutez vos scripts ici
});
